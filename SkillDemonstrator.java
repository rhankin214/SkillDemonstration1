import static org.junit.Assert.*;
import org.junit.*;

public class SkillDemonstrator {
    public boolean isEven(int input)
    {
        if(input % 2 == 0)
        {
            return true;
        }
        else
        {
            return false;
        }
    }
}